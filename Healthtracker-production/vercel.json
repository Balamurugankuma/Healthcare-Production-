{
  "version": 2,
  "builds": [
    {
      "src": "health-frontend/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "build" }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "health-frontend/$1"
    }
  ]
}
